# TypeScript Runtime Boundary

This directory owns TypeScript-specific runtime helpers for the shipped language pack.

In the current migration slice, these runtime subdirectories establish the owned boundary required by `RULES.md` while the generated project template remains the primary exercised surface.
