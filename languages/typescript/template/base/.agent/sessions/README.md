# Sessions

This directory is reserved for persisted local session state in generated TypeScript projects.

The current migration slice establishes the owned session boundary without claiming the full upstream session runtime surface.
