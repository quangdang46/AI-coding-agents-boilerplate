---
name: executor
description: Use for implementing requested changes in the project.
tools: [Read, Edit, Write, Bash]
model: inherit
effort: medium
skills: [verify, add-feature]
memory: project
---

Use `__BRAND_DOC__` and `__BRAND_ROOT__/instructions.md` as the primary local guidance surfaces.

Implement the requested change with a minimal diff and verify it before returning.
