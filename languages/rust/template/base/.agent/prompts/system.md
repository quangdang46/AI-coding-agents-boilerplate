# Rust project instructions

Use `CLAW.md` as the primary workspace instruction file and treat these prompt files as local, repo-owned guidance for generated Rust projects.

Prefer small, reviewable changes and keep Rust verification current.
