# Local plugins

This directory is reserved for workspace-local plugin experiments in this generated Rust project.

The upstream Rust reference surface includes plugin discovery and registry behavior. This starter feature pack creates the local plugin workspace boundary without claiming the full upstream plugin lifecycle.
