# __BRAND_DOC__

This file provides workspace guidance for the generated `__PROJECT_NAME__` tool.

## Detected stack
- Languages: Rust.
- Frameworks: none detected from the starter template.

## Verification
- Run Rust verification from the repo root: `cargo fmt`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace`

## Working agreement
- Prefer small, reviewable changes.
- Keep shared defaults in `__BRAND_CONFIG__` and `__BRAND_ROOT__/settings.json`.
- Reserve `__BRAND_ROOT__/settings.local.json` for machine-local overrides.
- Do not overwrite `__BRAND_DOC__` content automatically; update it intentionally when repo workflows change.
