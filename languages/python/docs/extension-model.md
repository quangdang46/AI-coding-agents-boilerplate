# Python Language Pack Extension Model

The Python proving slice follows the shared extension model defined by `RULES.md`:

1. runtime defaults
2. project-local `.agent/*` assets
3. enabled feature-pack assets
4. explicit CLI overrides
