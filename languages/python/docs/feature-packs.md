# Python Language Pack Feature Packs

The Python language pack currently uses project-local feature packs under:

- `.agent/features/registry.json`
- `.agent/features/<feature-id>/feature.json`

The proving-slice sample feature is `github-pr-review`.
