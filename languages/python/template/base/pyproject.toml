[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "__PROJECT_NAME__"
version = "0.1.0"
description = "Generated Python coding-agent project"
requires-python = ">=3.11"

[project.scripts]
__BINARY_NAME__ = "__PACKAGE_NAME__.cli:main"
