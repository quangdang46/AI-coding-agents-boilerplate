# Coding Agent With MCP Example

Use this example shape when you want a generated Python agent that keeps the standard coding workflow but enables MCP-backed features through feature packs.

Suggested flow:
1. scaffold with `aicd init ... --language python`
2. add feature packs with `aicd feature add ...`
3. validate with `aicd doctor --project <dir>`
