# Source TypeScript Reference Alias

This directory is the normalized migration alias for archived TypeScript source evidence.

The currently preserved archived material lives under `references/typescript/`. Use that tree for the underlying archived files until the archive layout is fully normalized.

This alias exists so migration docs can refer to `references/source-typescript/` as the stable source-oriented identity while keeping shipped runtime ownership separate under `install/` and `languages/`.
