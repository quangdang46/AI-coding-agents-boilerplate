# Source Rust Reference Alias

This directory is the normalized migration alias for archived Rust source evidence.

The currently preserved archived material lives under `references/rust/`. Use that tree for the underlying archived files until the archive layout is fully normalized.

This alias exists so migration docs can refer to `references/source-rust/` as the stable source-oriented identity while keeping shipped runtime ownership separate under `install/` and `languages/`.
